# Dylan W. Lim GitHub Profile Docs

Purpose: Public profile README and profile art assets.

Rules:

- Public-facing documentation only. Do not include credentials, private implementation details, unreleased strategy, user data, provider secrets, internal roadmap, or private repo paths.
- Treat `README.md`, `DOCS_MAP.md`, `PUBLIC_CONTENT_RULES.md`, and `RELEASE_STATUS.md` as the canonical docs map.
- Keep product status honest and update stale links when the source app changes.
- This repo has no package scripts unless a future docs build system is intentionally added.
- Validate docs-only changes with link/path checks and `git status`.
